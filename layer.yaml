includes: ['layer:basic', 'layer:apt']  # if you use any interfaces, add them here
options:
  apt:
    packages:
     - openjdk-8-jre-headless
  basic:
    python_packages:
      - mcstatus
repo: "https://github.com/erik78se/layer-minecraft-server"
